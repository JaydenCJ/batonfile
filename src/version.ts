/** Single source of truth for the tool version reported by `--version`. */
export const VERSION = "0.1.0";

/** Major version of the batonfile interchange format this build reads and writes. */
export const FORMAT_VERSION = "1";
